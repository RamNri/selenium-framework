[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*

addopts = -m "not artifact_validation"

markers =
    artifact_validation: intentional failure test used to validate the failure artifact pipeline